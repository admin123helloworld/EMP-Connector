public class Car {
    public Car(Integer x){
        system.debug('Value is ='+x);
    }
    public Car(Decimal d){
        system.debug('Value of Decimal is ='+d);
    }
    public Car(string Name){
        system.debug('Name of Car is ='+Name);
    }
}